import React from 'react';

import CustomCarousel from '../components/CustomCarousel';

const MainPage = () => {
  return (
    <div className="page">
      <CustomCarousel />
    </div>
  );
};

export default MainPage;
